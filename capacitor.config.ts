import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elzondao',
  appName: 'app-student',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
